import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.octaslash.engineer',
  appName: 'Falcon',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
